plugins {
    id 'androidx.build.java-library'
    id 'androidx.build.publishing'
}

globalConfig {
    pomName = 'Android ConstraintLayout Solver'
    pomDescription = 'Solver for ConstraintLayout'
}

dependencies {
    testImplementation "junit:junit:4.13.1"
}

